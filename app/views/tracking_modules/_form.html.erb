<%= form_for(@tracking_module) do |f| %>
  <% if @tracking_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tracking_module.errors.count, "error") %> prohibited this tracking_module from being saved:</h2>

      <ul>
      <% @tracking_module.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
